---
description:
globs:
alwaysApply: false
---
# 项目目录结构说明

## 主要目录
- `config/` - 存放训练配置文件
- `sd-models/` - Stable Diffusion 基础模型存放目录
- `output/` - 训练输出目录，包含生成的 LoRA 模型
- `logs/` - 训练日志目录
- `scripts/` - 辅助脚本目录
- `mikazuki/` - GUI 后端实现
- `frontend/` - GUI 前端实现
- `assets/` - 项目资源文件
- `huggingface/` - Hugging Face 相关配置

## 关键文件位置
- 训练脚本：项目根目录下的 `train.*` 文件
- GUI 启动脚本：根目录下的 `run_gui.*` 文件
- 安装脚本：根目录下的 `install.*` 文件
- 配置示例：`config/` 目录下的 TOML 文件
